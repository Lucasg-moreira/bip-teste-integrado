server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

ejb:
  url: http-remoting://localhost:8080
  context: org.wildfly.naming.client.WildFlyInitialContextFactory